<%= render @property %>

<div>
  <%= link_to "Edit", edit_property_path(@property), class: "btn btn-primary" %> 
  <%= link_to "Back", properties_path, class: "btn btn-secondary" %>

  <%= button_to "Delete", @property, method: :delete, class: "btn btn-danger mt-2" %>
</div>
