<%= form_with(model: property_type) do |form| %>
  <% if property_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(property_type.errors.count, "error") %> prohibited this property_type from being saved:</h2>

      <ul>
        <% property_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :property_type, style: "display: block" %>
    <%= form.text_field :property_type %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
